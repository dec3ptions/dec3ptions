/*
// EQUIP DAGGER //

dagger_selection.equipped.push("battle_axe");
await sync.async_set("dagsel_equipped", JSON.stringify(dagger_selection.equipped));
await dagger_selection.init();
*/

/*
// UNEQUIP ALL //

await sync.async_set("dagsel_equipped", JSON.stringify([]));
await dagger_selection.init();
*/

